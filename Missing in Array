def missingNumber(self, n, arr):
        
        # code here
        arr_set = set(arr)
        
        maxi = max(arr)
        
        for i in range(1,maxi):
            if i not in arr_set:
                return i
                
        return maxi + 1
